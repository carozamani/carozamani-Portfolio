import { AdminLoginModule } from '@/features/admin/components/AdminLoginModule';

export const metadata = { title: 'Admin Login', robots: { index: false } };

export default function Page() {
  return <AdminLoginModule />;
}
