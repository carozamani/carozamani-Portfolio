/** Where the full episode library lives; the site itself only shows the latest episodes. */
export const podcastHub = {
  name: 'Castbox',
  // Replace with the real channel URL.
  url: 'https://castbox.fm/',
} as const;
